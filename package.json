{
  "name": "@mhp/api-response",
  "version": "0.0.2-1",
  "description": "API Response Class",
  "main": "Response.js",
  "repository": "https://github.com/mh/node-api-response",
  "author": "mh",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "mocha test/unit test/int",
    "test:unit": "mocha test/int",
    "test:int": "mocha test/unit",
    "test:coverage": "nyc _mocha test/unit test/int && nyc report -r html && nyc check-coverage",
    "test:watch": "nodemon -x mocha test/unit test/int",
    "release:test": "set -uex; yarn test; npm version prerelease; npm publish --tag testing --access restricted",
    "release:patch": "set -uex; rm -rf node_modules; yarn; yarn run test:coverage; npm version patch; git push; npm publish --access restricted; git push --tags"
  },
  "dependencies": {
    "@mhp/ClassDebug": "~0.0.2",
    "@mhp/api-message": "0.0.3",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.15.0",
    "mocha": "^4.0.1",
    "nyc": "^11.3.0"
  },
  "nyc": {
    "per-file": true,
    "lines": 99,
    "statements": 99,
    "functions": 99,
    "branches": 99
  }
}
